rules:
  no-print:
    description: Disallow bare print() in production Python code
    engine: script
    scope: "**/*.py"
    severity: warning
    script: "grep -n 'print(' {file} && exit 1 || exit 0"
